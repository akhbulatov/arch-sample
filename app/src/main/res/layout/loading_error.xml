<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <import type="android.text.TextUtils" />

        <variable
            name="loadingError"
            type="String" />
    </data>

    <android.support.constraint.ConstraintLayout
        android:id="@+id/loadingErrorLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        app:visible="@{!TextUtils.isEmpty(loadingError)}"
        tools:visibility="visible">

        <TextView
            android:id="@+id/loadingErrorTextView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:text="@{TextUtils.isEmpty(loadingError) ? @string/error_unknown : loadingError}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="@string/error_unknown" />

    </android.support.constraint.ConstraintLayout>

</layout>